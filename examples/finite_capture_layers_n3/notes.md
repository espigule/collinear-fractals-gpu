# Finite-capture layers for n=3

This example illustrates the early finite-capture filtration for \(n = 3\),
where \(N = 2n-1 = 5\).

- Parameter: \(c = 0.5 + 1.1i\).
- Expected verdict: `Interior` for the selected marked-point search.
- Status: illustrative; compact certificate examples live in
  `examples/trap_enclosure_n3/`.
- Related work: the 2026 finite-capture filtration and canonical trap
  iteration figures.

To reproduce, open the browser explorer, select this example, and switch the
comparison mode to escape strata or overlay mode to inspect finite-search
depth layers.

The visualization explains the filtration geometry. Theorem-level proofs do
not rely on visual inspection.
