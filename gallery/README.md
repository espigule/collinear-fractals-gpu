# Gallery

This directory is reserved for small curated gallery assets and captions.
Large render dumps should not be committed. Prefer compressed WebP, SVG, or
PNG assets only when they are tied to metadata in `examples/` or
`paper_figures/`.

Planned gallery entries:

| Asset | Source example | Status |
|---|---|---|
| `hero_finite_capture_n13.webp` | `examples/hole_zoom_n13` | planned |
| `e_c4_overlap.webp` | `examples/e_c4_overlap` | planned |
| `e_c5_plane_filling.webp` | `examples/e_c5_plane_filling` | planned |
| `theta0_base_capture.svg` | `examples/theta0_base_capture` | planned |
| `trap_enclosure_certificate.svg` | `examples/trap_enclosure_n3` | planned |
| `threshold_n20_lens.webp` | `examples/threshold_n20` | planned |

Recommended caption:

```markdown
Generated with Collinear Fractals GPU. The image illustrates the
finite-capture structure; the theorem-level proof relies on the
trap-enclosure inequalities and finite certificates, not on visual inspection.
```
